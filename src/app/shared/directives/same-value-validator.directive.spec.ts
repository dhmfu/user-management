import { SameValueValidatorDirective } from './same-value-validator.directive';

describe('SameValueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SameValueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
